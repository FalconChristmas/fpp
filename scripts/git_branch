#!/bin/sh

BINDIR=$(dirname $0)
. ${BINDIR}/common

if [ "x$1" = "x" ]
then
	echo "USAGE: $0 BRANCHNAME"
	exit 1
fi

logOutput

echo "Switching git branch to $1"
if [ "x${FPPDIR}" = "x/opt/fpp" ]
then
	cd ${FPPDIR} && sudo git checkout $1
else
	cd ${FPPDIR} && git checkout $1
fi

